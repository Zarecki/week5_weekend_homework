### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb') #'./card.rb'
class CardGame


  def checkforAce(card)
    if card.value = 1 #double equal
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2) #should be def, not dif. Comma between arguments.
  if card1.value > card2.value
    return card.name #no card specified. Cards have no name.
  else
    card2 #do what with card2?
  end
end
end #one too many ends.

def self.cards_total(cards) #cards does not exist. pass two cards and define cards as a variable.
  total #what does total equal? is it a string, an intiger?
  for card in cards
    total += card.value
    return "You have a total of" + total #return should be outside loop
  end
end

#requires and end here to close the class.
```
